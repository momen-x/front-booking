import { LucideIcon } from "lucide-react"
export interface ISteps {
  step: string;
  title: string;
  description: string;
}

export interface IFeature {
    icon: LucideIcon;
    title: string;
    description: string;
}
